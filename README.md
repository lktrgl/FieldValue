# FieldValue
How to start:
```
git clone --recurse-submodules --verbose --remote --progress https://github.com/lktrgl/FieldValue.git \
    && cd VentCtrlStm8 \
    && mkdir build \
    && cd build \
    && cmake .. \
    && make all
```
